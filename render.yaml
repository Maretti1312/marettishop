services:
  - type: web
    name: telegram-shop-bots
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python customer_bot.py & python admin_bot.py & wait
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0